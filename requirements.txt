# App runs on Python 3.12 (set in Streamlit Cloud → Settings → Python version).
# Pinned to 1.58.0 — width='stretch' and st.html() are supported here.
# Bump this pin only after verifying the next release doesn't break APIs.
streamlit==1.58.0
PyPDF2>=3.0
pandas>=2.0,<4
numpy>=1.26,<3
openpyxl>=3.0
xlrd>=2.0
reportlab>=4.0
pdfplumber>=0.10
psycopg2-binary>=2.9
pycryptodome>=3.19   # AES decryption for encrypted PDFs (Lotus, newer e-statements)
